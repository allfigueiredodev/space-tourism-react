import React from "react";
import { DotStyle } from "./styles/dot/dot.styled";

export default function Dot({id, onClick}) {
    return (
        <DotStyle id={id} onClick={onClick}>
            
        </DotStyle>
        
    )
} 